# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: tronflow-vt-1.0.0
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bioconda::vt=2015.11.10
  - bioconda::bcftools=1.9